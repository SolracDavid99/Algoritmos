function arraysExercise3(){
    numbers = [1,2,3,4];
    var sum = numbers[0] + numbers[1] + numbers[2] + numbers[3];
    var multi = numbers[0] * numbers[1] * numbers[2] * numbers[3];
    console.log("The sum of the array is " + sum);
    console.log("The multiplication of the array is " + multi);
    return;
    }
    arraysExercise3();